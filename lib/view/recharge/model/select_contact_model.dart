import 'dart:ui';

class SelectContactModel {
  String? name;
  String? number;
  String? title;
  Color? color;
  Color? textColor;

  SelectContactModel({
    this.name,
    this.number,
    this.color,
    this.textColor,
    this.title,
  });
}